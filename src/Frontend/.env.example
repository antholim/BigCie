# API Configuration
# Leave empty or omit to use the same origin as the frontend (recommended for production)
# VITE_API_BASE_URL=http://localhost:8080/

# WebSocket Configuration
# Leave empty or omit to use the same origin as the frontend (recommended for production)
# VITE_WS_URL=ws://localhost:8080/ws

# Note: For production (e.g., https://bigcie.cimpo1.com):
# - The frontend will automatically use https://bigcie.cimpo1.com/ for API calls
# - The frontend will automatically use wss://bigcie.cimpo1.com/ws for WebSocket
# 
# For local development (e.g., http://localhost:5173):
# - The frontend will automatically use http://localhost:5173/ for API calls
# - But you likely want to override to point to your backend:
#   VITE_API_BASE_URL=http://localhost:8080/
#   VITE_WS_URL=ws://localhost:8080/ws
